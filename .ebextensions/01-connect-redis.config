option_settings:
  aws:elasticbeanstalk:application:environment:
    REDIS_HOST: "your-redis-host"  # Elastic Cache Redis의 엔드포인트를 여기에 입력

container_commands:
  01_restart_application:
    command: "sudo service httpd restart"
    leader_only: true